"use strict";

const {
  INTENT_RULES,
  detectBusinessAnalysis,
  detectIntent,
  routeSkillIntent,
} = require("./router");

module.exports = {
  INTENT_RULES,
  detectBusinessAnalysis,
  detectIntent,
  routeSkillIntent,
};
